# VRChat OSC Keyboard の使い方

このドキュメントでは、VRChat OSC Keyboardの基本的な起動方法や操作手順について説明します。

VRChat OSC Keyboardは **VR専用** のソフトウェアです。ご利用には **SteamVR** が必要です。

---

## 起動方法

ソフトウェアのインストールが完了している場合、以下のいずれかの方法で起動できます。

* **スタートメニュー**から「VRChat OSC Keyboard」を選択
* **デスクトップのショートカット**をダブルクリック

起動すると、**デスクトップには設定ウィンドウ**が表示され、**キーボードは VR 内**に表示されます。

* **SteamVRより先に起動した場合**：設定ウィンドウの上部に「SteamVRを起動してください」と表示されます。そのままSteamVRを起動すれば、再起動しなくても自動でVR内にキーボードが表示されます。
* **アプリの終了**：設定ウィンドウを閉じると、アプリ全体が終了します。
* **SteamVR を途中で終了した場合**：本ツールも一度終了し、SteamVRを起動し直してから本ツールを起動してください。

---

## 基本操作

### キーボードの表示 / 非表示
SteamVR（VRChat）のプレイ中に、**右コントローラーのスティックを押し込む**ことでキーボードの表示状態を切り替えることができます。

### 入力方法
VRコントローラーのポインティング動作（レイを照射して選択）で、画面上のキーを入力します。

> **物理キーボードでの入力について**
> v2.3.0 から、物理キーボードでの入力はできなくなりました。キーボードを VR 内だけに描画して表示の遅れを減らしているため、Windows の入力フォーカスを受け取れないためです。

### 設定の変更
テーマや言語、OSCポートなどの設定は、デスクトップの**設定ウィンドウ**から変更できます。変更はVR内のキーボードにもすぐに反映されます。

---

## その他

* **チャットウィンドウの操作が不要**
  VRChat内でチャットウィンドウを毎回手動で開く必要はありません。本ツール上で入力を行うだけで、そのままテキストの入力と送信が完了します。
